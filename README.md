# Sanchit PA OS

This is an Obsidian personal assistant vault seeded from the PA Codex project.

It keeps the reusable personal-assistant operating pattern:

- Obsidian vault folders for work notes, projects, goals, meetings, and reference.
- Hidden assistant brain with memory, commands, skills, hooks, and session logs.
- TaskNotes and Dataview as the core Obsidian plugins.

## Current Setup Status

Created locally in Codex at:

`C:\Users\sanch\Documents\Codex\2026-07-28\utcursh-creator-pinto-git-https-github\outputs\pinto-pa-os`

This vault has been seeded with:

- Base PA operating requirements for Sanchit Nagpal.
- OTT / One Take Tall operating memory.
- OTT Google Sheets workflow and task state.
- Migration/setup tasks that still need GitHub/Obsidian completion.

## Open In Obsidian

1. Open Obsidian.
2. Choose `Open folder as vault`.
3. Select the `pinto-pa-os` folder above.
4. When prompted, trust the vault only if you are comfortable enabling the included assistant configuration.

## Required Obsidian Plugins

Install these from Obsidian Settings -> Community Plugins -> Browse:

| Plugin | Status | Purpose |
| --- | --- | --- |
| TaskNotes | Required | Task database, task views, time tracking |
| Dataview | Required | Query notes and project/task metadata |
| Calendar | Recommended | Daily note navigation |
| Excalidraw | Optional | Whiteboard and diagrams |
| Style Settings | Optional | Theme customization |

## GitHub Status

The connected GitHub account is:

`sanchitnagpalofficial-create`

The target repo name checked was:

`sanchitnagpalofficial-create/pinto`

That repo does not currently exist, and the available GitHub connector in this Codex session does not expose repository creation or fork creation. The local shell also does not have `git` or `gh` available.

Once GitHub write access is available, push this vault to:

`https://github.com/onetaketall-hub/PA`

## Main Commands

- `/setup`: personalize or revise the vault.
- `/resume`: load memory, recent sessions, tasks, and current work.
- `/compress`: save a session log before closing.
- `/preserve`: save permanent rules or durable project knowledge.
- `/daily-review`: morning, evening, or weekly review workflow.

## Cleanup Status

This repo state intentionally excludes the original creator's personal notes, PDFs, research folders, card/content files, memories, project context, and user-specific data. It keeps only the reusable assistant operating logic and Sanchit's approved PA/OTT memory.
